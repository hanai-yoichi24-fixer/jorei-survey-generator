# CLAUDE.md — 条例調査表ジェネレーター

## このリポは何か
屋外広告物申請の条例調査表ドラフトを自動生成する**単一ファイルの静的Webアプリ**（`index.html`）。
フレームワーク・ビルド・バックエンドなし。Vercel で静的配信。

## 触る場所
- 判定ロジック・規制値：`index.html` の `<script>` 内 `KB` オブジェクト
- 看板種別・壁面の選択肢：`TYPES` / `WALLTYPES`
- 壁面基準面積の既定：`WALL_BASE`
- 丸亀プリセット：`PRESET`
- 出力（調査表・面積計算表）の描画：`generate()`

## 原則
- 完全クライアントサイド。入力データを外部送信・保存しない。
- 「要確認（warn）」は人的判断に残す箇所。自動で「適合」に倒さない。
- 規制値を変えたら出典（しおり／手引きの版）も併せて更新する。

## 更新フロー
clone → 編集 → `git push`（main）→ Vercel 自動再デプロイ。
**再度 deploy-app を実行しない。** 詳細は DEPLOY.md。
